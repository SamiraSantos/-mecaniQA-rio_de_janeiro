# A porta 8081 do Ubuntu/WSL será encaminhada ao NodePort 30080 da API.
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 30080
        hostPort: 8081
        protocol: TCP
